version: '3.8'

# 개발 서버에 DB와 Redis가 이미 구축되어 있으므로
# 이 파일은 참고용으로만 남겨둡니다.
# 실제 서비스는 외부 DB와 Redis를 사용합니다.

# 필요시 로컬 개발 환경 구축을 위해 아래 주석을 해제하여 사용할 수 있습니다.
#
# services:
#   redis:
#     image: redis:7-alpine
#     container_name: tiny_breakers_redis
#     ports:
#       - "6379:6379"
#     volumes:
#       - redis_data:/data
#     command: >
#       redis-server
#       --requirepass ${REDIS_PASSWORD:-tiny_breakers_redis_password}
#       --appendonly yes
#     restart: unless-stopped
#     networks:
#       - tiny_breakers_network
#
#   postgres:
#     image: postgres:15-alpine
#     container_name: tiny_breakers_postgres
#     environment:
#       POSTGRES_USER: ${DB_USER:-postgres}
#       POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
#       POSTGRES_DB: ${DB_NAME:-tiny_breakers_db}
#     ports:
#       - "5432:5432"
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     restart: unless-stopped
#     networks:
#       - tiny_breakers_network
#
# volumes:
#   redis_data:
#     driver: local
#   postgres_data:
#     driver: local
#
# networks:
#   tiny_breakers_network:
#     driver: bridge
